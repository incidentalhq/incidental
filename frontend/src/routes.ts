export enum RoutePaths {
  LOGIN = "/login",
  DASHBOARD = "/",
  REGISTER = "/register",
  REGISTER_SUCCESS = "/register/success",
  OAUTH_COMPLETE = "/oauth/complete",

  INCIDENTS = "/incidents",
  SETTINGS = "/settings",
}
