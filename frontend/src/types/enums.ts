export enum IncidentRoleKind {
  REPORTER = 'REPORTER',
  LEAD = 'LEAD'
}

export enum IncidentStatusCategory {
  TRIAGE = 'TRIAGE',
  ACTIVE = 'ACTIVE',
  POST_INCIDENT = 'POST_INCIDENT',
  CLOSED = 'CLOSED'
}
